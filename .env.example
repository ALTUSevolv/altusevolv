# Browser-safe values only. Never expose privileged/service-role secrets with a
# browser-visible prefix.

# Phase 1: managed Supabase public configuration.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Future server-only variables are added only when the feature is implemented.
# Examples: database/service-role credentials, email provider secrets, payment
# provider secrets and AI provider keys. Do not invent or commit real secrets.
